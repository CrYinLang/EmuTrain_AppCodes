# 2026-06-19 Daily Notes

## emutrain Flutter 项目大重构 (00:10 - ~02:10)

### 完成的工作

**1. logError 全面覆盖**
- 写了 Python 脚本自动给所有 catch 块添加 `logError()` 调用
- 102 个 catch 块添加了 logError
- 清理了 4 个文件中的重复 logError 调用
- 涉及文件：config/、models/、providers/、services/、widgets/、screens/ 全部 dart 文件

**2. 模块目录拆分 (>15KB 文件)**
- 将 21 个文件从 `lib/screens/` 迁移到 12 个模块目录
- 模块：journey(4文件,251KB)、route(5,163KB)、search(2,97KB)、settings、gps、map、gallery、station、travel、record、about、welcome、tools
- 用 Python 脚本批量修复了所有 import 路径（处理了 screens/ → module/ 的相对路径变化）
- `error.dart` 保留在 `lib/widgets/error.dart`，`tool_screen.dart` 迁移到 `lib/tools/`

**3. Visual Studio Build Tools 安装**
- 安装了 .NET SDK 8.0.422
- 安装了 VS Build Tools 2022 + VCTools workload + Windows SDK 10.0.22621.0
- MSVC cl.exe 就位于 `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\cl.exe`

**4. Flutter Windows 桌面编译**
- `flutter create --platforms=windows .` 添加了 Windows 支持
- `flutter build windows` 成功编译
- 输出：`build\windows\x64\runner\Release\emutrain.exe` (Win32 应用，非 UWP)

**5. 2 小时自动关机**
- 用户要求设置 2 小时后强制关机，然后去睡觉
- 已设置 `shutdown /s /t 7200 /f`，之后因任务提前完成又执行了 `shutdown /s /t 30 /f`

### 最终状态
- **dart analyze**: 0 errors, 36 info warnings
- **Windows build**: 成功
- **旧 lib/screens/ 目录**: 已删除
- **Python 脚本**: 已清理（fix_*.py, split_*.py, refactor_all.py 等临时脚本已删除）

### 遇到的问题
- PowerShell `Out-File -Encoding utf8` 输出 git show 结果时会损坏编码（BOM 或 UTF-16LE），导致 Dart 解析失败产生 2000+ errors
- 从 git 恢复后代码库干净，重新做拆分工作正常
- Dart 无法用 `part`/`part of` 拆分单个类（类体必须在一个文件中），所以 journey.dart 保持为单文件
- 相对路径 import 修复是最大的工作量——文件从 screens/ 或 screens/function/ 移到 module/ 后，所有相对 import 都需要重新计算

### 用户相关信息
- 用户偏好：大工具装 F 盘（463.7GB 可用），C 盘仅 17.3GB
- 用户去睡觉了，电脑已关机
- 项目版本：1.2.31+1231，Flutter 3.41.7 stable，Dart SDK ^3.10.7
